services:
  - type: web
    name: edupulse
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: RENDER_EXTERNAL_URL
        sync: false  # tu le remplis manuellement dans le dashboard
